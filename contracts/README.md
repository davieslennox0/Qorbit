# contracts

Solidity contracts for Quatripe. Deploy to Arc testnet (chainId `5042002`, RPC `https://rpc.testnet.arc.network`) using Hardhat, Foundry, or Viem — tooling to be added once the first contract is written.
